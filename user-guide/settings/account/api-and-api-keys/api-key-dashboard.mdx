---
title: "API Key Dashboard"
description: "View, create, edit, regenerate and archive API keys used for authentication. This article covers the supported authentication methods: Basic and OAuth 2.0."
keywords: ["authentication type", "API key authentication type", "MoEngage API key", "create an API key", "multi-API key", "multikey", "API key rotation", "legacy API key", "Basic Auth (Legacy)", "OAuth 2.0 access token expiration", "API key permissions", "archived API keys", "API key audit logs"]
---

<Note>
  This is an Early Access feature. To enable it for your account, please contact your MoEngage Customer Success Manager (CSM) or the Support team.
</Note>

## Overview

API keys are typically used as an authentication or authorization mechanism. They are unique identifiers associated with access to API resources.

API keys help ensure the security and accountability of API access, allowing you to manage and monitor usage effectively.

The multi-API key feature is currently supported for the following API groups:

| API Group | Supported Authentication Types |
| --- | --- |
| [Data APIs](/api/data/data-overview) | Basic Auth |
| [Segmentation APIs](/api/custom-segments/custom-segments-overview) | Basic Auth |
| [Push APIs](/api/push/push-overview) | Basic Auth (Legacy) |
| [Campaigns](/api/campaigns/campaigns-overview) | Basic Auth, OAuth 2.0 |
| [Inform](/api/inform/inform-overview) | Basic Auth |
| [Offerings](/api/offerings/offerings-overview) | Basic Auth, OAuth 2.0 |
| Decision Policy | Basic Auth, OAuth 2.0 |
| [Dashboard & Analyze](/api/analytics/analytics-overview) | Basic Auth, OAuth 2.0 |

<Note>
 - Data APIs, Segmentation APIs, and Inform are available only with Basic Auth.
 - Creation of new API keys for Push APIs is not currently supported. Push APIs work only with **Basic Auth (Legacy)** keys migrated to this dashboard.
</Note>

To send data to MoEngage, you can use the Workspace ID and key for that particular API. Whenever a key is regenerated, the old key expires immediately and it takes some time for the new key to become functional, which may result in service interruptions. MoEngage provides multiple API keys to help you avoid this situation:

- **Generate a new key while the existing key is still available**: With multi-key support, the existing key remains active until the new key becomes functional. This allows you to rotate keys for security purposes without causing downtime. For example, if your team needs time to integrate a new key, the old key continues to work during that period. After the new key is integrated, you can archive the old one.
- **Use different keys for different teams**: You can create separate keys for different internal teams or external vendors, each scoped to the APIs they need. This provides granular access control and improves security across your organization.

To restrict which IP addresses can use your keys, see [API Firewall](/user-guide/settings/account/security/api-firewall).

## Authentication Types

MoEngage supports two authentication types for API keys:

- **Basic Auth**: The standard authentication method using an API key and secret. Suitable for server-to-server integrations where token expiration is not a requirement.
- **OAuth 2.0**: A token-based authentication method that issues short-lived access tokens. The token expiry time is customizable, and the backend validates access tokens automatically.

## Create a Key

<Info>
  - After the multi-API key feature is enabled, the existing API keys in the API dashboard are migrated to your new API key dashboard as legacy keys.
  - You can view the legacy key (autogenerated previously when the workspace was created and migrated to this new dashboard) details and continue to use them. However, archiving, editing, and regenerating legacy keys is not supported.
  - MoEngage recommends creating and using new keys, as legacy keys will be deprecated in the future.
</Info>

<Steps>
  <Step title="Open the API Keys Page">
    On the left navigation menu in the MoEngage dashboard, go to **Settings** \> **Account** \> **API keys**.
  </Step>
  <Step title="Start a New Key">
    Click **+ Create new key**.

    <Frame><img src="/images/create-new-key.png" alt="Create new key button highlighted on the API Keys page" /></Frame>
  </Step>
  <Step title="Open the Key Details Form">
    In the **Create new key** dialog, enter the key details.
  </Step>
  <Step title="Name the Key">
    Enter a name in the **Key name** box. The key name must not exceed 100 characters.
  </Step>
  <Step title="Choose an Authentication Type">
    Under **Authentication Type**, select the method you want to use, then set the options for that method:

    <Tabs>
      <Tab title="Basic Auth">
        Under **Select APIs for access**, select the API groups you want to access using this key:

        - Data APIs
        - Segmentation APIs
        - Campaigns
        - Inform
        - Offerings
        - Decision Policy
        - Dashboard & Analyze

       <Frame><img src="/images/basicauth.png" alt="Image showing the steps to create a new API key in the Create new key dialog of Basic Authentication" /></Frame>
 
      </Tab>
      <Tab title="OAuth 2.0">
        1. In the **Access token expiration (in minutes)** box, enter the token lifetime. The value must be between 5 and 60 minutes, and the default is 15 minutes.

           <Info>
             This box appears only when **Authentication Type** is set to **OAuth 2.0**.
           </Info>

        2. Under **Select APIs for access**, select the API groups you want to access using this key:
           - Campaigns
           - Offerings
           - Decision Policy
           - Dashboard & Analyze

         <Frame><img src="/images/oAuth20.png" alt="Image showing the steps to create a new API key in the Create new key dialog for OAuth 2.0" /></Frame>

        **Data APIs**, **Segmentation APIs**, and **Inform** are not listed for OAuth 2.0 keys. To access these groups, create a Basic Auth key.
      </Tab>
    </Tabs>
  </Step>
  <Step title="Scope the API Access">
    Click <Icon icon="plus" /> next to a group to expand it and select individual APIs. Use **Select all** or **De-select all** to manage selections across all groups.
  </Step>
  <Step title="Save the New Key">
    Click **Create key**. The key is then listed under **Active API keys**.
  </Step>
</Steps>

To use the key in requests, see [Authentication](/api/introduction#authentication). For per-endpoint request limits and payload caps, see [Rate Limits](/api/rate-limits).

## View Active Keys

On the **API Keys** page, the **Active API keys** table shows the following columns:

| Column | Description |
| --- | --- |
| Key name | The name assigned to the key. |
| Auth type | **Basic Auth**, **OAuth 2.0**, or **Basic Auth (Legacy)** for migrated legacy keys. |
| Created on | The date and time the key was created, along with the email address of the user who created it. |
| Key | The masked key value with a view button and a copy button. |
| Actions (<Icon icon="ellipsis-stroke-vertical" />) | Options to manage the key. See [Manage Keys](#manage-keys). |

You can search for keys using the search bar. The count of active keys is displayed above the table.

<Note>
  Legacy keys are listed with the auth type **Basic Auth (Legacy)**. You can view and continue to use these keys, but you cannot edit, regenerate, or archive them.
</Note>

## Manage Keys

On the **API Keys** page, click <Icon icon="ellipsis-stroke-vertical" /> for a key to see the following options: <Frame><img src="/images/manage-key.png" alt="Key action menu showing View key details, Regenerate key, Edit key details, and Archive key options" /></Frame>

| Option | Description |
| --- | --- |
| [View key details](#view-key-details) | View the full details of the key. From the details panel, you can also access **Edit key details** and **Regenerate key**. |
| Regenerate key | Generate a new key. |
| [Edit key details](#edit-key-details) | Edit the key's name or API access settings. |
| Archive key | Archive the key. Archived keys cannot be unarchived. A maximum of 10 archived keys is maintained in the archived key list. |

<Warning>
  The old key expires immediately after regenerating. Replace the old key in your integrations to avoid errors.
</Warning>

### View Key Details

<Steps>
  <Step title="Open the Key Actions Menu">
    On the **API Keys** page, click <Icon icon="ellipsis-stroke-vertical" /> for the key you want to view.
  </Step>
  <Step title="Open the Details Panel">
    Click **View key details**. The fields in the details panel depend on the authentication type of the key:

    <Tabs>
      <Tab title="Basic Auth">
        | Field | Description |
        | --- | --- |
        | Key name | The name assigned to the key. |
        | API key | The masked key value with a view button and a copy button. |
        | Authentication type | **Basic Auth**, or **Basic Auth (Legacy)** for a migrated legacy key. |
        | Last updated on | The date and time the key was last modified. |
        | Last updated by | The email address of the user who last modified the key. |
        | Created on | The date and time the key was created. |
        | Created by | The email address of the user who created the key. |
        | API access | The APIs selected for this key, with checkmarks indicating the enabled options. Click <Icon icon="plus" /> next to a group to see the individual APIs it includes. |
      </Tab>
      <Tab title="OAuth 2.0">
        | Field | Description |
        | --- | --- |
        | Key name | The name assigned to the key. |
        | API key | The masked key value with a view button and a copy button. |
        | Authentication type | **OAuth 2.0**. |
        | Access token expiration | The token lifetime in minutes. |
        | Last updated on | The date and time the key was last modified. |
        | Last updated by | The email address of the user who last modified the key. |
        | Created on | The date and time the key was created. |
        | Created by | The email address of the user who created the key. |
        | API access | The APIs selected for this key, with checkmarks indicating the enabled options. Click <Icon icon="plus" /> next to a group to see the individual APIs it includes. |
      </Tab>
    </Tabs>
  </Step>
</Steps>

From the details panel, you can click **Edit key details** or **Regenerate key** to perform those actions.

<Frame><img src="/images/edit-regenerate-keyy.png" alt="View key details panel showing key name, API key, authentication type, access token expiration, audit fields, and API access, with the Edit key details and Regenerate key buttons highlighted" /></Frame>

### Edit Key Details

<Steps>
  <Step title="Open the Key Actions Menu">
    On the **API Keys** page, click <Icon icon="ellipsis-stroke-vertical" /> for the key you want to edit.
  </Step>
  <Step title="Open the Edit Dialog">
    Click **Edit key details**.
  </Step>
  <Step title="Update the Fields">
    In the **Edit key details** dialog, edit the available fields:

    <Tabs>
      <Tab title="Basic Auth">
        | Field | Editable |
        | --- | --- |
        | Key name | Yes |
        | Authentication Type | No |
        | Select APIs for access | Yes |
      </Tab>
      <Tab title="OAuth 2.0">
        | Field | Editable |
        | --- | --- |
        | Key name | Yes |
        | Authentication Type | No |
        | Access token expiration | Yes |
        | Select APIs for access | Yes |
      </Tab>
    </Tabs>
  </Step>
  <Step title="Save Your Changes">
    Click **Save**. The **Save** button remains disabled until you make a change.
  </Step>
</Steps>

### View Archived Keys

1. On the **API Keys** page, click **View archived keys**.
2. The archived keys list opens with the following columns: Key name, Auth type, Archived on, and Key.

<Frame><img src="/images/view-archived-key-page.png" alt="Archived keys list showing key name, authentication type, created on, and key columns" /></Frame>

## Audit Logs

<Frame><img src="/images/auditt-logs.png" alt="Audit logs page showing API key actions" /></Frame>

Use [Audit Logs](/user-guide/settings/account/audit-logs/audit-logs) to track actions performed in the MoEngage dashboard. The audit logs include all actions related to API keys, so you can identify who performed each action and when. You can do the following in **Audit logs**:

- Click <Icon icon="bars-filter" /> to filter audit logs based on **Team(s)**, **Action**, **Actor**, and **Date time Range**.
- Type a value in the **Search logs by** search bar to fetch the relevant audit logs.
- Click **Export audit logs** to receive the audit log report by email. The email includes a download link for the report.

For all actions related to API keys, the **Entity** column in **Audit logs** displays **API Keys**. The **Action** column shows the following values:

- **Create** is displayed when an API key is created successfully.
- **Update** is displayed when:
  - A key name is updated.
  - The API selection for a key is updated.
  - A key is regenerated.
- **Delete** is displayed when a key is archived.

## FAQs

<AccordionGroup>
  <Accordion title="What are legacy keys?">
    Legacy keys are API keys created automatically when the workspace was created and migrated from the older API keys dashboard to this one. You cannot edit, regenerate, or archive them.
  </Accordion>

  <Accordion title="What is access token expiration in OAuth 2.0?">
    OAuth 2.0 access token expiration is a security feature that limits how long an access token can be used to call protected APIs before it becomes invalid.

    An access token acts as a temporary digital pass. Instead of sharing your password with every app or service, the authorization server gives you a short-lived token. When that token reaches its expiration time, the API rejects any further requests and returns an `HTTP 401 Unauthorized` error.
  </Accordion>

  <Accordion title="Why does access token expiration matter?">
    - **Better security**: If an attacker steals a short-lived token, they have only a brief window to exploit it before it stops working.
    - **Resource control**: Expiration prevents unused or abandoned tokens from filling up the authorization server's database.
    - **Standard lifespan**: You can set the access token expiration anywhere from 5 to 60 minutes.
  </Accordion>

  <Accordion title="How does MoEngage handle expired tokens?">
    MoEngage issues a companion refresh token alongside the access token. Your app uses this longer-lived refresh token in the background to request a fresh access token, without forcing the user to log in again.
  </Accordion>
</AccordionGroup>
